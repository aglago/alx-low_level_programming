#include "main.h"

/**
 * _isdigit - chrcks if c is digit
 * @c: input parameter to check against
 * Return: always 0
 */

int _isdigit(int c)
{
	c = 48;
	if (c <= 57)
		return (1);
	else
		return (0);
	return (0);
}
